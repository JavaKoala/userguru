<%= form_for(@comment) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :text, placeholder: "Compose new comment...." %>
  </div>
  <%= hidden_field_tag :issue_id, @issue.id %>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>